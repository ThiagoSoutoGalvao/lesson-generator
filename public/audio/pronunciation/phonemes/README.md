# Phoneme audio files

One MP3 per phoneme, named exactly by its IPA symbol (matches the `audio`
field in `resources/js/data/pronunciation/phonemes.json` and
`soundCards.json`).

Examples: `iː.mp3`, `ɪ.mp3`, `θ.mp3`, `tʃ.mp3`, `ɜː.mp3`

44 files total (12 monophthongs, 8 diphthongs, 24 consonants) — see
`phonemes.json` for the full symbol list. Native British English (RP) speaker,
clear and consistent volume. Sourced in Phase 2 — not committed yet.
